# Built output, checked in for consumers that install these packages
# directly (e.g. via a file: dependency) without running a build step.
# Marked vendored so GitHub's language stats and diff/PR review UI treat it
# as generated/vendored code, not hand-written source.
packages/components-react/dist/** linguist-vendored
packages/tokens/dist/** linguist-vendored
